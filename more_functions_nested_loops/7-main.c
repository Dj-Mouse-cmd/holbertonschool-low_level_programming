#include "main.h"

/**
 * main - check the code for print_diagonal function
 *
 * Description: Tests the print_diagonal function with
 * different values of n.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}

