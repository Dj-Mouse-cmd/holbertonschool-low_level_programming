#include "main.h"

/**
 * main - check the code for print_line function
 *
 * Description: This program tests print_line with different values.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_line(0);
	print_line(2);
	print_line(10);
	print_line(-4);
	return (0);
}

