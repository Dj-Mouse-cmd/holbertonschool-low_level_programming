#include "main.h"

/**
 * main - check the code for more_numbers function
 *
 * Description: This program tests the more_numbers function,
 * which should print 10 times the numbers from 0 to 14,
 * each sequence followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	more_numbers();
	return (0);
}

