#include "main.h"

/**
 * main - check the code for print_square function
 *
 * Description: Tests the print_square function with different sizes.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	print_square(2);
	print_square(10);
	print_square(0);
	return (0);
}

